#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * Description - prints all single digit numbers of base 10 starting from 0
 * Return: 0 (Success)
 */
int main(void)
{
	int i;

	while (i = 0; i < 10; i++)
	{
		putchar('0' + i);
	}
	putchar('\n');
	return (0);
}
