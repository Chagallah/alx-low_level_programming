#include "main.h"

/**
 * mul(int a, int b) - function that multiplies integers
 * @a: int to be multiplied to b
 * @b: int to be multiplied to a
 * Return: output.
 */
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
